/* eslint-disable @typescript-eslint/ban-ts-comment  */
// @ts-ignore
import RenameManager from './renameManager.svelte';

// @ts-ignore
const renameManager = new RenameManager({
  target: document.body
});

export default renameManager;
